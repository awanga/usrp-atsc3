# lib/sync/ - Synchronization primitives
# Bootstrap detection, frame sync, timing recovery

set(SYNC_SOURCES
    # bootstrap_detector.cc
    # frame_sync.cc
    # timing_recovery.cc
)

set(SYNC_HEADERS
    # bootstrap_detector.h
    # frame_sync.h
    # timing_recovery.h
)

# Sources will be added to parent LIB_SOURCES when implemented
